@model IEnumerable<letterhead.Models.uservm>
@{
    ViewBag.Title = "ApproverMaster";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int i = 0;
}

<div class="pagetitle">
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="#">Masters</a></li>
            <li class="breadcrumb-item active">Approver</li>
        </ol>
    </nav>
</div>
<section class="section dashboard">
    <div class="row">
        <div class="col-12">
            <div class="card recent-sales overflow-auto">
                <div class="filter" style="right:23px;">
                    @*<button type="button" class="btn btn-primary"><i class="bi bi-star me-1"></i> With Text</button>*@
                    <a class="btn btn-primary" href="@Url.Action("AddApprover", "Home")"><i class="bi bi-person-plus"></i>Add Approver</a>
                    <a class="btn btn-danger" href="@Url.Action("Dashboard","Home")">Cancel</a>
                </div>
                <div class="card-body">
                    <h5 class="card-title">Approver Master</h5>

                    <table class="table table-borderless datatable">
                        <thead>
                            <tr>
                                <th scope="col">S.No</th>
                                <th scope="col">EMP-CODE</th>
                                <th scope="col">Full Name</th>
                                <th scope="col">Mobile</th>
                                @*<th scope="col">User Name</th>*@
                                <th scope="col">Password</th>
                                <th scope="col">Can Login</th>
                                <th scope="col">Site Detail</th>
                                <th scope="col">Dept. Name</th>
                                <th scope="col">Status</th>
                                <th scope="col">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                i = i + 1;
                                <tr>
                                    <th scope="row"><a href="#">@i</a></th>
                                    <td>@item.EMPCODE</td>
                                    <td>@item.FULLNAME</td>
                                    <td>@item.MOBILENO</td>
                                    <td>@item.USERPWD</td>
                                    <td><span class="badge bg-@(item.CANLOGIN==true?"success":"danger")">@(item.CANLOGIN==true? "Active" : "De-Active")</span></td>
                                    <td>@(item.TITLE+"/"+item.SITENO + "/" + item.SITENONAME)</td>
                                    <td>@item.Department</td>
                                    <td><span class="badge bg-@(item.ISACTIVE==true?"success":"danger")">@(item.ISACTIVE==true? "Active" : "De-Active")</span></td>
                                    <td><a href="@Url.Action("EditApprover", "Home", new {id=@item.ID})"><i class="bi bi-pencil"></i></a></td>
                                </tr>
                            }

                        </tbody>
                    </table>

                </div>

            </div>
        </div>
    </div>
</section>

@Html.Partial("_Alerts")


